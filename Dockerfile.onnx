FROM registry.access.redhat.com/ubi10-micro:latest

# Copy the ONNX model to /models directory
COPY --chown=1001:0 models/mlserver/onnx/model.onnx /models/model.onnx

# Set proper privileges for KServe
RUN chmod -R a=rX /models

# Use non-root user as default
USER 1001
